includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6

    paths:
        - src
        - database

    # The package ships without a full Laravel app; Larastan provides the
    # framework stubs it needs to analyse the source in isolation.
    checkModelProperties: true
